name: example
description: Demo of window_paint.
publish_to: "none"
version: 1.0.0+2

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  window_paint:
    path: ../
  flutter_colorpicker: ^1.1.0
  uuid: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
