name: example
description: Demo of window_paint.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=2.12.0-133.2.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  window_paint:
    path: ../
  flutter_colorpicker: ^0.4.0-nullsafety

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.10.0-nullsafety

flutter:
  uses-material-design: true
